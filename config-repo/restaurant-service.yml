server:
  port: 8082
spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/restaurant_db
    username: quickbite
    password: quickbite
  jpa:
    hibernate:
      ddl-auto: validate